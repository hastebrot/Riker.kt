## Riker.kt

A test framework.

### Reference

| TDD style        | BDD style                        |
|------------------|----------------------------------|
| `suite()`        | `describe()`, alias: `context()` |
| `test()`         | `it()`                           |
| `suiteSetup()`   | `before()`                       |
| `suiteCleanup()` | `after()`                        |
| `testSetup()`    | `beforeEach()`                   |
| `testCleanup()`  | `afterEach()`                    |
